Backend (server/package.json)
Added new easy-to-use commands:

npm run dev:full - Runs backend + frontend together (recommended for development)
npm run start:full - Runs both in production mode
npm run build:full - Builds and runs production version

Frontend (client/package.json)
Enhanced with type-check script for TypeScript validation
Existing scripts remain unchanged
